# Office/Admin in-app password (drivers do not use this).
app_password = "versa2026"

# Streamlit Cloud → App settings → Sharing → "This app is public and searchable"
# If the app is private, Streamlit blocks ALL users (including drivers) before the app loads.